# 数据融合扩展指南

## 📊 当前框架架构

当前框架是一个**通用的时间-性能评分散点图可视化系统**，具有以下特点：

### 核心数据结构
```json
{
  "id": "唯一标识",
  "name": "数据点名称",
  "date": "时间（YYYY-MM-DD）",
  "value": "性能评分（数值）",
  "cluster": "分类/聚类（用于颜色分组）",
  "domain": "领域（用于形状区分）",
  "conditions": "实验条件/描述",
  "result": "结果描述",
  "source": "数据来源/论文/报告",
  "sourceUrl": "来源链接（可选）",
  "codeUrl": "代码仓库链接（可选）",
  "githubUrl": "GitHub链接（可选）",
  "huggingfaceUrl": "HuggingFace链接（可选）",
  "paperUrl": "论文链接（可选，兼容旧字段）"
}
```

### 字段说明
- **id**: 数据点的唯一标识符
- **name**: 数据点的显示名称
- **date**: 时间戳，用于 X 轴
- **value**: 数值，用于 Y 轴（可以是性能评分、心率、温度等）
- **cluster**: 分类/聚类，用于颜色分组（如"RLHF (传统派)"、"健康监测"）
- **domain**: 领域类型，用于形状区分（如"general"、"reasoning"、"health"）
- **conditions**: 条件/环境描述（如"运动后30分钟"、"SFT + RM + PPO"）
- **result**: 结果描述
- **source**: 数据来源（论文标题、报告名称、设备名称等）
- **sourceUrl**: 来源的直接链接
- **codeUrl**: 代码仓库链接
- **githubUrl**: GitHub 链接
- **huggingfaceUrl**: HuggingFace 链接
- **paperUrl**: 论文链接（保留以兼容现有数据）

### 可视化维度
- **X轴**：时间（date）
- **Y轴**：性能评分（value）
- **颜色**：按 cluster 分组
- **形状**：按 domain 区分

---

## 🔄 如何融合心率数据（示例）

### 方案一：在同一图表中融合（推荐）

#### 1. 扩展数据结构
在 `data.json` 中添加心率数据，使用 `domain` 字段区分数据类型：

```json
{
  "id": "heartrate_2024_01_15",
  "name": "心率记录 - 2024-01-15",
  "date": "2024-01-15",
  "value": 72,
  "cluster": "健康监测",
  "domain": "health",  // 新增领域类型
  "conditions": "运动后30分钟",
  "result": "静息心率正常，运动恢复良好",
  "source": "手环记录",
  "sourceUrl": "https://health-app.com/record/12345"
}
```

#### 2. 扩展配置
在 `alignment-chart.html` 的 `CONFIG` 中添加：

```javascript
const CONFIG = {
    colors: {
        // 原有分类
        'RLHF (传统派)': '#e3b341',
        // ... 其他分类
        
        // 新增分类
        '健康监测': '#ff6b6b',
        '运动数据': '#4ecdc4',
        '睡眠数据': '#95e1d3'
    },
    shapes: {
        'general': 'circle',
        'reasoning': 'triangle',
        'health': 'rect',  // 新增形状
        'fitness': 'diamond'  // 新增形状
    }
}
```

#### 3. 扩展 Y 轴标签
根据数据类型动态调整 Y 轴标签：

```javascript
// 检测数据类型
const hasHealthData = rawData.some(item => item.domain === 'health');
const yAxisName = hasHealthData ? '性能评分 / 心率 (bpm)' : '性能评分';
```

---

### 方案二：创建独立的数据源和图表

#### 1. 创建新的数据文件
创建 `health-data.json`：

```json
[
  {
    "id": "heartrate_2024_01_15",
    "name": "心率记录 - 2024-01-15",
    "date": "2024-01-15",
    "value": 72,
    "cluster": "静息心率",
    "domain": "heartrate",
    "conditions": "运动后30分钟",
    "result": "静息心率正常",
    "source": "手环记录",
    "sourceUrl": "https://health-app.com/record/12345"
  }
]
```

#### 2. 创建新的导航页面
在导航体系中添加健康数据入口：

```
工科 → 生物医学工程 → 健康监测 → 心率数据
```

#### 3. 复用图表组件
可以创建一个通用的图表组件，通过配置参数来适配不同数据类型。

---

## 🎯 融合策略建议

### 1. **数据分类体系扩展**

在导航体系中添加新的分类路径：

```
工科
├── 计算机科学 → 人工智能 → 大模型 → 后训练对齐（当前）
├── 生物医学工程 → 健康监测 → 心率数据（新增）
├── 生物医学工程 → 健康监测 → 睡眠数据（新增）
└── 生物医学工程 → 健康监测 → 运动数据（新增）
```

### 2. **数据结构扩展**

为不同类型的数据添加扩展字段：

```json
{
  // 基础字段（必需）
  "id": "...",
  "name": "...",
  "date": "...",
  "value": "...",
  "cluster": "...",
  "domain": "...",
  
  // 扩展字段（可选，根据数据类型）
  "dataType": "health|research|fitness",  // 数据类型标识
  "unit": "bpm|score|percentage",  // 数值单位
  "metadata": {  // 元数据
    "device": "手环型号",
    "user": "用户ID",
    "location": "位置信息"
  },
  
  // 链接字段（可选）
  "source": "数据来源",
  "sourceUrl": "来源链接",
  "codeUrl": "代码链接",
  "githubUrl": "GitHub链接",
  "huggingfaceUrl": "HuggingFace链接"
}
```

### 3. **可视化适配**

#### 多 Y 轴支持
如果数据单位不同，可以使用双 Y 轴：

```javascript
yAxis: [
    {
        type: 'value',
        name: '性能评分',
        position: 'left'
    },
    {
        type: 'value',
        name: '心率 (bpm)',
        position: 'right'
    }
]
```

#### 动态图例
根据数据动态生成图例和颜色配置。

---

## 📝 实施步骤

### 步骤 1：数据准备
1. 将心率数据转换为 JSON 格式
2. 确保数据结构符合基础字段要求
3. 添加 `domain` 和 `cluster` 字段进行分类

### 步骤 2：配置扩展
1. 在 `CONFIG` 中添加新的颜色和形状配置
2. 扩展 `domain` 映射关系
3. 调整 Y 轴标签和范围

### 步骤 3：导航扩展（可选）
1. 在 `home.html` 中添加"生物医学工程"入口
2. 创建健康监测相关页面
3. 添加路由到图表页面

### 步骤 4：详情页面适配
在详情模态框中，根据 `dataType` 显示不同的字段：
- 科研数据：显示论文、代码链接
- 健康数据：显示设备信息、用户信息

---

## 🔧 代码示例

### 扩展 CONFIG 配置

```javascript
const CONFIG = {
    jsonUrl: `../data.json?v=${Date.now()}`,
    colors: {
        // 原有分类
        'RLHF (传统派)': '#e3b341',
        'RLAIF (AI反馈)': '#58a6ff',
        // ... 其他
        
        // 健康数据分类
        '健康监测': '#ff6b6b',
        '静息心率': '#ff8787',
        '运动心率': '#ffa8a8',
        '睡眠数据': '#95e1d3'
    },
    shapes: {
        'general': 'circle',
        'reasoning': 'triangle',
        'health': 'rect',
        'heartrate': 'diamond',
        'sleep': 'star'
    },
    // 新增：数据类型配置
    dataTypes: {
        'research': {
            yAxisName: '性能评分',
            yAxisMin: 2,
            yAxisMax: 11
        },
        'health': {
            yAxisName: '心率 (bpm)',
            yAxisMin: 40,
            yAxisMax: 200
        }
    }
}
```

### 动态 Y 轴配置

```javascript
// 检测数据类型并设置 Y 轴
function getYAxisConfig(data) {
    const hasHealth = data.some(item => item.domain === 'health');
    const hasResearch = data.some(item => item.domain === 'general' || item.domain === 'reasoning');
    
    if (hasHealth && hasResearch) {
        // 双 Y 轴
        return [
            { type: 'value', name: '性能评分', position: 'left', min: 2, max: 11 },
            { type: 'value', name: '心率 (bpm)', position: 'right', min: 40, max: 200 }
        ];
    } else if (hasHealth) {
        return { type: 'value', name: '心率 (bpm)', min: 40, max: 200 };
    } else {
        return { type: 'value', name: '性能评分', min: 2, max: 11 };
    }
}
```

### 详情页面适配示例

```javascript
// 在详情模态框中根据数据类型显示不同内容
function renderDetailContent(item) {
    const isResearch = item.domain === 'general' || item.domain === 'reasoning';
    const isHealth = item.domain === 'health';
    
    let sourceSection = '';
    if (isResearch) {
        sourceSection = `
            <a href="${item.sourceUrl || item.paperUrl || `https://arxiv.org/search/?query=${encodeURIComponent(item.source || item.paper)}`}" target="_blank">
                📄 论文/来源: ${item.source || item.paper}
            </a>
        `;
    } else if (isHealth) {
        sourceSection = `
            <div>📱 数据来源: ${item.source}</div>
            ${item.sourceUrl ? `<a href="${item.sourceUrl}" target="_blank">查看详情</a>` : ''}
        `;
    }
    
    return sourceSection;
}
```

---

## ✅ 总结

**当前框架已经具备良好的扩展性**，可以融合不同类型的数据：

1. ✅ **数据结构灵活**：通过 `cluster` 和 `domain` 字段可以灵活分类
2. ✅ **可视化通用**：时间-数值散点图适用于多种数据类型
3. ✅ **导航体系可扩展**：可以添加新的分类路径
4. ✅ **详情页面可适配**：可以根据数据类型显示不同内容
5. ✅ **字段命名通用**：使用 `source` 和 `sourceUrl` 替代 `paper` 和 `paperUrl`，更通用

**建议**：
- 对于相似的数据类型（如科研数据），可以直接融合到同一图表
- 对于差异较大的数据类型（如心率 vs 科研），建议创建独立的数据源和图表页面
- 使用 `domain` 字段区分数据类型，使用 `cluster` 字段进行细分分类
- 优先使用 `source` 和 `sourceUrl` 字段，保留 `paper` 和 `paperUrl` 以兼容现有数据

